schema_version: "1.0"
generated_at: "20260618205130"
variables:
  SC1_DONE: {type: bool, nullable: false}
  SC2_DONE: {type: bool, nullable: false}
  SC3_DONE: {type: bool, nullable: false}
  SC4_DONE: {type: bool, nullable: false}
  SC5_DONE: {type: bool, nullable: false}
  SC6_DONE: {type: bool, nullable: false}
  SC7_DONE: {type: bool, nullable: false}
  SC8_DONE: {type: bool, nullable: false}
  SC9_DONE: {type: bool, nullable: false}
  SC10_DONE: {type: bool, nullable: false}
invariants:
  # SC-1 (table exists) is prerequisite for SC-2 (log_event writes) and SC-4 (migration events logged)
  - "z3.Implies(SC2_DONE == True, SC1_DONE == True)"
  - "z3.Implies(SC4_DONE == True, SC1_DONE == True)"
  # SC-2 (log_event) is prerequisite for SC-3 (log_exception wraps log_event), SC-7 (DB resilience), SC-9 (get_events)
  - "z3.Implies(SC3_DONE == True, SC2_DONE == True)"
  - "z3.Implies(SC7_DONE == True, SC2_DONE == True)"
  - "z3.Implies(SC9_DONE == True, SC2_DONE == True)"
  # SC-3 (log_exception) is prerequisite for SC-5 (handle_ui_error calls log_exception)
  - "z3.Implies(SC5_DONE == True, SC3_DONE == True)"
  # SC-5 is prerequisite for SC-8 (backward-compat of handle_ui_error)
  - "z3.Implies(SC8_DONE == True, SC5_DONE == True)"
  # SC-6 (stderr preserved) is independent
  # SC-10 (init.py docstring-only) is independent
