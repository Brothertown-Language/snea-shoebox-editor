schema_version: "1.0"
generated_at: "20260618205130"
variables:
  PHASE1_DONE: {type: bool, nullable: false}
  PHASE2_DONE: {type: bool, nullable: false}
  PHASE3_DONE: {type: bool, nullable: false}
invariants:
  # Phase 1 (model + migration method): SystemEventLog model and migration method
  # Phase 2 (service): EventLogService with log_event, log_exception, get_events
  # Phase 3 (integration): Update migration log functions + handle_ui_error
  # Service needs model
  - "z3.Implies(PHASE2_DONE == True, PHASE1_DONE == True)"
  # Integration needs service
  - "z3.Implies(PHASE3_DONE == True, PHASE2_DONE == True)"
