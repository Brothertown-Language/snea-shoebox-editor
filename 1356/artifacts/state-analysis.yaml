spec: https://github.com/Brothertown-Language/snea-shoebox-editor/issues/1356
generated_at: 2026-07-16T00:00:00Z
state_analysis:
  phases:
    - phase: "1 — Delete retired workflow docs"
      pre_state:
        - docs/git-workflow-migration-guide.md exists on disk
        - docs/streamlit-dev-workflow.md exists on disk
      post_state:
        - docs/git-workflow-migration-guide.md removed from working tree
        - docs/streamlit-dev-workflow.md removed from working tree
      state_transitions:
        - file_exists → file_deleted (git rm)
    - phase: "2 — Update doc references from dev to main"
      pre_state:
        - AGENTS.md contains dev exception line 52
        - docs/security/credential-leakage-remediation.md line 84 references origin dev
      post_state:
        - AGENTS.md has dev exception line removed
        - docs/security/credential-leakage-remediation.md line 84 references origin main
      state_transitions:
        - line_present → line_removed (edit)
        - dev_ref_present → main_ref_present (edit)
    - phase: "3 — Verification sweep"
      pre_state:
        - Possible remaining \`dev\` workflow references in docs/
      post_state:
        - Zero \`dev\` branch workflow references in docs/
      state_transitions:
        - reference_present_or_absent → reference_absent (verification only — no state change)
